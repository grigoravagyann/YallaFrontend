/**
 * PLACEHOLDER — replace by running `pnpm api:generate`.
 *
 * This file is normally generated from the backend's OpenAPI document by
 * `openapi-typescript` and committed, so the workspace typechecks without a
 * running backend. It is checked in as a stub because the backend was not
 * reachable when the monorepo was scaffolded.
 *
 * Do not hand-edit. Regenerate with:
 *
 *   pnpm api:generate --url https://localhost:7188/swagger/v1/swagger.json
 */

export interface paths {
  [path: string]: unknown;
}

export interface components {
  schemas: Record<string, unknown>;
}

export interface operations {
  [operation: string]: unknown;
}

/** True until the real document has been generated. */
export const IS_PLACEHOLDER_SCHEMA = true;
