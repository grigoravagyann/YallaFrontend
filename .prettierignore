node_modules
dist
dist-web
build
.expo
pnpm-lock.yaml
packages/api/src/generated

# Playwright's output from a local e2e run; gitignored in apps/e2e, so CI never sees it.
apps/e2e/test-results
apps/e2e/test-results-*
apps/e2e/playwright-report
apps/e2e/blob-report

# Written by packages/tokens/scripts/generate-css.mjs; formatting it by hand would only make the next run rewrite it.
apps/web/src/tokens.generated.css

# gstack's own working directory: QA reports, screenshots and its local state.
# Gitignored, so CI never sees it — without this, `pnpm format:check` fails
# locally for anyone with gstack installed while passing in CI.
.gstack

# Agent worktrees and local launch config, never part of the build
.claude/

# Superdesign drafts: excluded locally via .git/info/exclude, so CI never sees
# them, but prettier does not read that file and fails format:check without this.
.superdesign/
